# Unified EMBED Toolkit

This directory contains the unified implementation described in
`../docs/unified-system-plan.md`. The retired repository-level legacy trees
have been removed; this package is the only retained runtime implementation.

## Development

Install or run tools from this directory so imports resolve to
`unified-system/src/embed_toolkit`.

```bash
python -m pytest
```
